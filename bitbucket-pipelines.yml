pipelines:
  branches:
    default:
      - step:
          name: Run tests
          image: python:3.9-slim-buster
          script:
            - pip install -r dev-requirements.txt
            - python -m pytest
